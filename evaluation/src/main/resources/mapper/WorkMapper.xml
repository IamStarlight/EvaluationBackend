<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http#{//mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.evaluation.mapper.WorkMapper">

<!--    List<Map<String,String>> getAllWorkInfoBySid(@Param("sid") String sid);-->
    <select id="getAllWorkInfoBySid" resultType="java.util.Map">
        SELET wid,cname,title,startTime,endTime,editStatus,evaStatus,url
        FROM homework,sc,course
        WHERE  homework.cid=sc.cid
        AND homework.cid=course.cid;
        AND sc.sid=#{sid}
    </select>

    <select id="getWorkInfoById" resultType="java.util.Map">
        select title,end_time as endTime,status,submit_number as submitNumber,number as classNumber
        from homework
        where number in (select number from course where cid=#{cid})
        and cid=#{cid}
        and wid=#{wid}
    </select>
</mapper>
